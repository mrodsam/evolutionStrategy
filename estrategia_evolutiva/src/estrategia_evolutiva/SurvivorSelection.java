package estrategia_evolutiva;

import java.util.LinkedList;

public class SurvivorSelection {
	
	public static void muLambda() {
		
		LinkedList<Individual> newPopulation = new LinkedList<Individual>();
		
		while(newPopulation.size() < Main.populationSize) {
			/*Recorrer la lista de offspring, coger el que tiene menor fitness y añadirlo a la nueva población
			 * eliminarlo de la lista y repetir hasta tener la población nueva completa*/
		}
		
		
	}

}
